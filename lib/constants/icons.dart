import 'package:flutter/material.dart';

//Flutter Icons package
//https://api.flutter.dev/flutter/material/Icons-class.html

const Icon darkModeButton = Icon(
  Icons.dark_mode,
  color: Colors.black,
  size: 50.0,
);

const Icon lightModeButton = Icon(
  Icons.light_mode,
  color: Colors.white,
  size: 50.0,
);

const Icon darkModeOpenInNew = Icon(
  Icons.open_in_new,
  color: Colors.white,
  size: 50.0,
);

const Icon lightModeOpenInNew = Icon(
  Icons.open_in_new,
  color: Colors.black,
  size: 50.0,
);

const Icon darkModeMenu = Icon(
  Icons.menu,
  color: Colors.black,
  size: 100.0,
);

const Icon lightModeMenu = Icon(
  Icons.menu,
  color: Colors.white,
  size: 100.0,
);
